import { solution as subject } from './find-max-in-dynamicmatris'

describe('#findMaxInDynamicmatris()', () => {
  // describe.each([
  //   { input: '(N=3, M=5)', expected: 9768 }
  //   // { input: '(N=3, M=5)', expected: 8728 }
  // ])("When '$input' is given", ({ input, expected }) => {
  //   it(`returns '${expected}'`, () => {
  //     expect(subject(input)).toBe(expected)
  //   })
  // })
  it('should be defined', () => {
    expect(subject).toBeDefined()
  })
})
