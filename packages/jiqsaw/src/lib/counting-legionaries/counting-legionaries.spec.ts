import { solution as subject } from './counting-legionaries'

describe('#countingLegionaries()', () => {
  // describe.each([
  //   // { input: [1, 2660], expected: 'bar' }
  //   // { input: 'foo', expected: 'bar' }
  // ])("When '$input' is given", ({ input, expected }) => {
  //   it(`returns '${expected}'`, () => {
  //     expect(subject(input)).toBe(expected)
  //   })
  // })
  it('is expected be defined', () => {
    expect(subject).toBeDefined()
  })
})
